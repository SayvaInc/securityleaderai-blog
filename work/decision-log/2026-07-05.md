# Decision Log — 2026-07-05

## 1. Located the audio and reviewed the local folder

**What happened:** Searched this repo (all branches), the container, and
Google Drive — the audio existed nowhere reachable. The user then shared a
Finder listing: everything lives on their Mac at
`/Volumes/2TBSSD/.../securityleaderai-blog/public/audio/pa-in/` (164.5 MB),
alongside the uncommitted June 22 code (visible in their `git status`:
`audio-player.tsx`, `generate-audio*.{mjs,py}`, `panjabi-audio-sop.md`, etc.
on their local `content/pa-in-uk-consistency` checkout).

**Review verdict on the folder (licensing-driven):**
- **Ship:** the 4 NotebookLM `<slug>-overview.mp3` files (~22.6 MB total).
  NotebookLM ToS does not claim output ownership.
- **Do NOT ship:** the slug-named read-aloud MP3s and the whole `samples/`
  folder — generated with MMS-TTS, which is **CC-BY-NC 4.0** (non-commercial
  only). They stay local until regenerated with Indic Parler-TTS
  (Apache-2.0). This is the standing "Do not retry" licensing rule.
- **Skip:** the 4 Gurmukhi-titled `.m4a` (raw NotebookLM exports — byte-level
  duplicates of the renamed overviews) and the `-overview.m4a` copies (the
  `.mp3` versions are half the size).

**Alternative considered:** hosting on Cloudflare R2/CDN per the June 21
architecture doc. Skipped for now — 22.6 MB in-repo is well within limits;
revisit if the audio library grows (R30 would need a CDN-origin allowance).

## 2. Wired the four overview mappings

**What changed:** the user pushed the 4 overview MP3s from their Mac
(commit `edb2625`, staged safely: files-without-mappings is a NOTICE, not an
error, by R30 design). This session then added to each of the 4 pa-in posts:
`audio_url: "/audio/pa-in/<slug>-overview.mp3"` + `audio_kind: "overview"`.

**Verification:**
- `npm run lint:content` — 0 errors; R30 clean both directions (4/4 mappings
  resolve, zero orphan files).
- `npx tsc --noEmit`, `npm run lint:code` — clean.
- Build — 43 pages; each of the 4 pa-in pages has exactly one `<audio>`
  (correct per-post MP3 source, `audio/mpeg`) and exactly one `<h1>`.

## 3. Gurdwara read-aloud published on owner's decision

**What changed:** the owner reviewed `gurdwara-charity-donation-fraud.mp3`
and directed publication; pushed from their Mac (`7d7d70e`). Wired as
`audio_kind: "read_aloud"` (verbatim narration → "ਇਹ ਲੇਖ ਸੁਣੋ" label, not
the overview label).

**Caveat, recorded for the audit trail:** this file is from the June 22
slug-named batch that the Linear note attributed to MMS-TTS (CC-BY-NC).
The engine for this specific file was not re-confirmed in-session; the
owner made the publish call after content review. If it is MMS output,
swap it for an Indic Parler-TTS regeneration (Apache-2.0) — the wiring
stays identical, only the file changes.

**Verification:** R30 5/5 mappings + 0 orphans, tsc/lint clean, 43-page
build, gurdwara page renders exactly one `<audio>` (read-aloud label,
correct source) and one `<h1>`.

## Gaps / Follow-ups

- `gurdwara-charity-donation-fraud` has **no overview** — 4 of 5 posts done;
  one more NotebookLM run closes it. The post renders without a player
  until then (by design, not an error).
- Read-aloud (`audio_kind: read_aloud`) tracks: regenerate with Indic
  Parler-TTS before shipping; MMS output is licensing-blocked.
- The June 22 uncommitted work on the user's Mac includes docs and scripts
  worth salvaging in a future session (`panjabi-audio-sop.md`,
  `generate-audio-parler.py`, `daily-decision-digest.md` — the latter may
  overlap with what this repo now has in CLAUDE.md/.claude/agents/).
- Optional: re-encode overviews to mono 64 kbps (~halves size) per the
  architecture doc's own spec, if bandwidth ever matters.
