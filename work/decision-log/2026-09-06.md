# Decision Log — 2026-09-06

## 1. Published the three free-tier security-rules hook posts

**What changed:** Added three new blog posts — `secrets-echo-guard.md`,
`agent-git-baseline.md`, `tool-poisoning-guard.md` — each pairing with a
same-day public GitHub repo (ai-secrets-echo-guard, ai-agent-git-baseline,
ai-tool-poisoning-guard) that ships a free security rule for Claude Code,
Codex, and Cursor. This is the "free tier" hook from the AI Coding Tool
Security Rules PMF plan: one gap, one rule, one blog post, one repo, run as
an A/B test across three candidates.

**Why these three, in this format:** rule content was pulled from already-
validated source material rather than written fresh — the
`claude-code-error-prevention` skill's Rule 13 (secrets echo, validated
highest-severity finding across 12 real AI-coding sessions), the
agent-playbooks three-layer architecture doc, and cited MCP tool-poisoning
research (OWASP MCP Top 10 MCP03, CVE-2025-54136, a 97.1%-defect-rate
academic study). Each post follows the existing content-rigor template
(H1 → italic hook → Executive Summary blockquote → H2 body → "Your next
move" → board talking points → author attribution) — matched against
`mcp-sentinel-scanner.md` as the closest existing example, not improvised.

**Alternative considered:** one combined post covering all three rules.
Skipped — the A/B test's whole point is isolating engagement per candidate,
so one post per candidate matches the isolated-repo decision made on the
GitHub side earlier today.

**What was skipped and why:** did not touch `src/lib/research.ts` (these are
blog posts, not research articles — no TypeScript entry needed). Did not add
audio (`audio_url`/`audio_kind`) — no NotebookLM/TTS overview exists yet for
this content; posts render without a player, by design, not an error.

**Verification:**
- `npm run lint:content` — 0 errors on all three new files (8 pre-existing
  notices on unrelated posts, untouched by this change).
- `npm run lint:code`, `npx tsc --noEmit` — clean.
- `npm run build` — 84 pages, all three new slugs present
  (`/blog/secrets-echo-guard`, `/blog/agent-git-baseline`,
  `/blog/tool-poisoning-guard`).
- `<h1>` count = 1 on all three rendered pages
  (`.next/server/app/blog/<slug>.html`), per the CLAUDE.md invariant.
- `git status --short` after commit showed a clean tree with only the
  intended 3 post files + this log entry staged.

**Not fixed, flagged instead — pre-existing `npm audit` finding:**
`npm audit --omit=dev` reports 5 high-severity vulnerabilities in `sharp`
(CVE-2026-33327/33328/35590/35591, inherited from libvips), an optional
transitive dependency Next.js 16 pulls in for image optimization. This
contradicts the "Known Accepted Risks: None as of June 20, 2026" line
elsewhere in this file. Confirmed via `git diff --stat package-lock.json`
(no diff) and `git show HEAD:package-lock.json | grep sharp` that this
predates this session — not introduced by adding these three posts. Per
"Patch first" policy, `npm audit fix` was not run as part of this change
(would touch unrelated dependency state outside this task's scope); flagging
here so the next session (or the human) runs it deliberately and re-verifies
the "Known Accepted Risks" line rather than assuming it's still accurate.

## Do not retry

- Do not assume `npm audit` is clean because the "Known Accepted Risks"
  section says so — it was stale as of this session. Always re-run it.

## Gaps / Follow-ups

- Fix or document the `sharp`/libvips high-severity findings above and
  update "Known Accepted Risks" with the real, current result.
- No NotebookLM audio overview exists yet for these three posts — optional,
  not required for publication.
- The A/B test itself (comparing GitHub stars/forks/clones + blog engagement
  across the three candidates) has not started; no numeric threshold set for
  declaring a winner yet, per the PMF plan's Open Question #4.
